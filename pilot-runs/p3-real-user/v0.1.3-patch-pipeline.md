# v0.1.3 Patch-Pipeline — Bridge-Pair p3-real-user-Output

**Erstellt:** 2026-04-29 post-bridge-close R28
**Quelle:** Bridge-Pair 8cbeaad0 Mapping-Phase R12-R26 + existing v0.1.3-Backlog
**Bilanz-File:** `bridge/bilanz_8cbeaad0.md`
**Anwendung:** out-of-pair in `/Users/paulad/session-bridge/` Cowork-Project (per ADR_0021 strict-separation, Plugin NICHT installiert)
**Total Estimated:** ~17-20h Self-Edit + Self-Test + Doku-Updates

---

## §0 Pre-Conditions für Plugin-Dev-Session

| # | Bedingung |
|---|---|
| 1 | Cowork-Project `session-bridge-plugin-dev` mit Working-Dir `/Users/paulad/session-bridge/` |
| 2 | KEIN Plugin installiert (ADR_0021 strict-separation) |
| 3 | Add-Dir auf `/Users/paulad/session-bridge/private-notes/` für Profile-Edits (optional) |
| 4 | Add-Dir auf `/Users/paulad/session-bridge/pilot-runs/p3-real-user/` für friction-log + Bilanz-Read |
| 5 | gh-CLI authentifiziert für Plugin-Updates / Marketplace-Submit |

## §1 Anwendungs-Reihenfolge (Phasen A-F)

```
Phase A (1-2h)   Quick-Wins Doku-Affordances
   ↓
Phase B (3-4h)   User-Visibility CRITICAL (F-RP-30/31)
   ↓
Phase C (3-4h)   Spec-Konsistenz CRITICAL (D-001/D-002, F-RP-29)
   ↓
Phase D (3-4h)   Sentinel + Mount (D-004/D-005-A)
   ↓
Phase E (2-3h)   State-Schema + Tests + Doku
   ↓
Phase F (2-3h)   Lower-Priority HIGH
```

**Begründung Reihenfolge:**
- Phase A erst: Erfolgs-Erlebnis + low-risk + parallel zu Inhalts-Verständnis
- Phase B + C parallel CRITICAL: Marketplace-Adoption-Blocker
- Phase D nach C: bridge-init/bridge-attach Pre-Flight bereits aufgeräumt
- Phase E nach D: Self-Tests prüfen alle vorigen Phasen
- Phase F optional bei Zeit-Reserve

---

# Phase A — Quick-Wins Doku-Affordances (~1-2h)

## A.1 D-003 F-RP-33 — `pre-allocated`-Pattern (~30min)

**Source-File:** `skills/bridge-handover/SKILL.md`

**Patch — neue Sektion §forward-pointer-rationale einfügen** (zwischen §Pre-Flight und §Ablauf):

````markdown
## §forward-pointer-rationale (Affordance-Documented)

decision-lock-Round darf rationale-File pointer enthalten der zum
Schreib-Zeitpunkt noch nicht existiert. Pflicht-Markierung im state.json:

```yaml
shared_artifacts:
  - path: bridge/artifacts/<file>.md
    owner: <role>
    status: pre-allocated   # NICHT active
    round_allocated: <N>    # decision-lock-Round
    purpose: "<beschreibung>"
```

Folge-Round (typischerweise N+1) materialisiert File und setzt:

```yaml
status: active
round_active: <N+1>
```

**Rationale:** entkoppelt formale decision-lock von substantieller
Artefakt-Materialisierung, verhindert Block-Schleife bei async-Schreiben.

**Anti-Pattern:** pre-allocated-Status ohne Materialisierungs-Plan in
Folge-Rounds = Forward-Pointer-Drift. Nach 3 Rounds ohne active-Status
WARN-Markierung in bridge-status (siehe bridge-status.md §forward-pointer-warning).

**Empirische Validierung:** bridge-pair p3-real-user R11 worker-decision-lock
mit pre-allocated annex; R12 advisor-Materialisierung mit status active.
````

**Acceptance:** SKILL.md enthält neue §forward-pointer-rationale-Sektion. Bestehende Sektionen unverändert.

**Self-Test:** keiner direkt nötig; Pattern ist Doku-Affordance.

---

## A.2 D-005 Sub-B F-RP-34 — Konvergenz-Skip-Konvention (~30min)

**Source-File:** `skills/bridge-handover/SKILL.md`

**Patch — neue Sektion §konvergenz-skip-rationale einfügen** (nach §forward-pointer-rationale):

````markdown
## §konvergenz-skip-rationale (Affordance-Documented)

Konvergenz-Kriterium aus eigener Pair-Round darf in nachfolgender Round
übersprungen werden, wenn Substanz-Konvergenz bilateral schon erreicht ist
(z.B. via Plan-Layer-Akzeptanz aus früherer Round).

**Pflicht-Markierung:** Skip-Round Body enthält `status_observations[]`-Eintrag:

```yaml
status_observations:
  - type: convergence_criterion_skip
    defined_in_round: <N>      # Round, die Kriterium definiert hat
    skipped_in_round: <M>      # diese Round
    skip_basis: "<Begründung — z.B. bilaterale Substanz-Konvergenz>"
    cycle_counter: <Anzahl Cycles seit Definition>
```

**Anti-Pattern:**
- Skip ohne Markierung = AP-08-Bypass-Verdacht (Konsens-Inszenierung)
- Skip-mit-Markierung = legitime Affordance

**Empirische Validierung:** bridge-pair p3-real-user R11 (Worker-Self-Bypass
eigener R8-Konvergenz-Kriterium-Definition).
````

**Plus: Pre-Flight 6 in §Pre-Flight-Sektion ergänzen**

```markdown
6. Konvergenz-Kriterium-Skip-Check (NEU v0.1.3):
   Wenn vorherige Round (N-1) Konvergenz-Kriterium definiert hat und
   diese Round (N) das Kriterium übersprungen hat:
   - Body MUSS status_observations[] mit type=convergence_criterion_skip enthalten
   - Bei missing → WARN "Konvergenz-Bypass ohne Markierung — AP-08-Verdacht"
```

**Acceptance:** SKILL.md hat §konvergenz-skip-rationale + Pre-Flight 6.

**Self-Test:** T17-NEU (negative case: Skip ohne Markierung → WARN erwartet).

---

## A.3 F-RP-19 BEOBACHTUNG — Visualization-Widget UX-Pattern (~30min)

**Source-File:** `commands/bridge-init.md`

**Patch — neue Sektion am Ende einfügen:**

````markdown
## UX-Pattern: Visualization-Widget für Argument-Erfassung

Bei missing required-Args nutzt bridge-init `mcp__visualize__show_widget`
mit elicitation-Form für strukturierte Eingabe. Pattern empfohlen für:

- `--topic` (free text)
- `--shared-path` (file path picker mit Default-Heuristik)
- `--expertise-source` (free text)
- `--expertise-profile` (dropdown mit Lookup auf `expertise-profiles/`-Verzeichnisse)
- `--worker-session-id` (dropdown mit Lookup via session_info MCP)

**Empirische Validierung:** bridge-pair p3-real-user R0 nutzte Visualization-Widget
für Argument-Erfassung; Form-Vollständigkeit hat Pre-Flight 5 Profile-Validation
ermöglicht.

**Anti-Pattern:** missing required-Args ohne Elicitation-Form ist Modell-abhängig
(siehe F-RP-32 hard-enforce-Patch in v0.1.3).
````

**Acceptance:** bridge-init.md hat UX-Pattern-Sektion.

---

# Phase B — User-Visibility CRITICAL (~3-4h)

## B.1 F-RP-31 CRITICAL — /bridge-status erweitern (~2h)

**Source-File:** `commands/bridge-status.md` + ggf. helper-tool

**Patch — Output-Format vollständig redesignen:**

````markdown
## Ablauf (NEU v0.1.3)

```python
# 1. State-Read
state = read_state(shared_path)

# 2. Eigene Rolle bestimmen
this_role = "advisor" if state.roles.advisor.session_id == this_session_id else "worker"
other_role = "worker" if this_role == "advisor" else "advisor"

# 3. Other-Session-Title via session_info MCP (graceful_degrade falls FAIL)
try:
    other_session_info = session_info.get(state.roles[other_role].session_id)
    other_title = other_session_info.title
    other_status = other_session_info.status  # running | idle
    other_last_activity = other_session_info.last_activity_min_ago
except:
    other_title = "(unbekannt)"
    other_status = "(degraded)"
    other_last_activity = None

# 4. Letzte 3 Rounds + Phase + Decision-Log + Blocker
last_rounds = state.rounds[-3:]
phase = state.phase
decision_count = len(state.decision_log)
open_blockers = state.open_blockers

# 5. Nächste erwartete Aktion inferieren aus letzter Round + Phase
next_action = infer_next_action(state, this_role)

# 6. Format Output
print_status_block(this_role, other_role, other_title, other_status,
                   other_last_activity, last_rounds, phase, decision_count,
                   open_blockers, next_action)
```

## Output-Format (NEU v0.1.3)

```
============================================================
BRIDGE-PAIR <pair_id> — Phase: <phase>
============================================================
Diese Session:    <role> (<title>)
Andere Session:   <other-role> (<other-title>)
                  Status: <running|idle> · Last activity: <X min ago>

Letzte 3 Rounds:
  R<n> [<type>]   <from>→<to>   <timestamp>   <bytes>
  R<n-1> [<type>] <from>→<to>   <timestamp>
  R<n-2> [<type>] <from>→<to>   <timestamp>

Decision-Log: <count> Decisions locked
Open Blockers: <count> (siehe state.open_blockers)

Nächste erwartete Aktion (this session):
  <inferred-action-text>

Mögliche Skill-Calls:
  /bridge-handover --type=<a|b|c>
  /bridge-close (wenn Pair-Lifecycle-Ende erreicht)
============================================================
```

## Polling-Hint (NEU v0.1.3 per F-RP-31 Patch 3)

Wenn `other_last_activity_min_ago > 30`:
```
HINWEIS: Other session inactive für >30 min.
Möglicherweise in Plan-Phase (siehe F-RP-29).
Erwäge Polling oder Worker-Probe-Round.
```
````

**Acceptance:**
- bridge-status zeigt Rolle + Title + last activity der anderen Session
- Letzte 3 Rounds als Tabelle
- Nächste erwartete Aktion inferiert
- Bei other-session-inactive >30min: Polling-Hint

**Self-Test:** T18-NEU positive case (Pair mit 3+ Rounds, Output enthält alle Felder).

**Aufwand:** ~2h (Skill-Logik + Output-Formatting + session_info-Integration).

---

## B.2 F-RP-31 Patch 4 — Skill-Mode-Marker (~30min)

**Source-Files:** `skills/bridge-worker/SKILL.md` + `skills/bridge-advisor/SKILL.md`

**Patch — Output-Pflicht-Header an alle Skill-Outputs:**

**bridge-worker SKILL.md am Anfang von §Ablauf:**
```markdown
**Pflicht-Output-Header (NEU v0.1.3):**
Jeder bridge-worker Skill-Output beginnt mit:
```
[bridge-worker mode]
```
Dies erlaubt User klare Mode-Identifikation, verhindert Role-Drift-Confusion (siehe F-RP-30).
```

**bridge-advisor SKILL.md analog:**
```markdown
**Pflicht-Output-Header (NEU v0.1.3):**
Jeder bridge-advisor Skill-Output beginnt mit:
```
[bridge-advisor mode | profile=<profile-name oder "none"> v<version>]
```
```

**Acceptance:** Beide Skills haben Pflicht-Header-Definition. Skill-Body-Anwendung in allen Code-Pfaden konsistent.

**Self-Test:** T19-NEU (Output-Capture, Header-Match).

---

## B.3 F-RP-30 CRITICAL — bridge-worker §Role-Boundary (~1.5h)

**Source-File:** `skills/bridge-worker/SKILL.md`

**Patch — neue Sektion §Role-Boundary einfügen** (nach §Pre-Flight):

````markdown
## §Role-Boundary (NEU v0.1.3, CRITICAL)

bridge-worker Skill operiert STRIKT im Worker-Modus. Folgende Aktionen sind
**advisor-exklusiv** und in worker-Skill-Output VERBOTEN:

- **Profile-pflicht-workflows ausführen** (z.B. anti-pattern-check-pre-counter,
  diagnose-frame-anwenden)
- **AP-Diagnosen schreiben** (z.B. "AP-07 detected", "AP-08 Verdacht")
- **Frame-Wahl-Argumentationen** (z.B. "F1.1 anwendbar weil...")
- **Methoden-Veto bei User-Skill-Triggern** (z.B. "decision-lock kann nicht
  jetzt erfolgen weil Profile-Methodik anders verlangt")

Worker-Skill-Funktion: **operative Execution + Status-Bericht**. Bei User-
Skill-Trigger → Pre-Flight + ausführen + State-Mutation + Status-Output.

**Anti-Pattern (aus F-RP-30 Pilot-Empirie p3-real-user):**

| Aktion | Erlaubt für Worker? |
|---|---|
| Body-Tags wie "AP-Check", "pflicht_workflow", "Frame-Wahl" | NEIN (advisor-mode-Drift) |
| Hypothesen-Diagnostik (H1/H2/H3) bei Visibility-Gap | NEIN (advisor-mode) |
| Decision-Lock-Veto mit pflicht_workflow-Begründung | NEIN (strict forbidden) |
| Counter-Punkte mit Substanz-Boden + Frame | OK (Worker-Counter ist erlaubt) |
| anti-pattern-check-pre-counter ausführen | NEIN (Profile-pflicht-workflow) |

**Pre-Flight-Erweiterung (NEU v0.1.3):**

Vor jedem bridge-worker Skill-Output: scan Body auf advisor-mode-Tags.
Falls gefunden → WARN:

```
WARNING: bridge-worker Body enthält advisor-mode-Tags (<liste>).
Worker-Boundary-Drift-Verdacht (F-RP-30). Überprüfe ob Body
operativ-Worker-Inhalt oder advisor-mode-Inhalt.
```

WARN nicht hard-FAIL — Worker-Skill kann noch funktional sein, aber User
bekommt Hinweis.
````

**Acceptance:**
- bridge-worker SKILL.md hat §Role-Boundary mit konkreten Anti-Pattern-Beispielen
- Pre-Flight-Scan auf advisor-mode-Tags implementiert
- WARN-Output bei Drift-Verdacht

**Self-Test:** T20-NEU (positive case: Worker-Body mit "AP-Check" → WARN expected).

**Aufwand:** ~1.5h (Sektion + Pre-Flight-Scan-Logik + Self-Test).

---

# Phase C — Spec-Konsistenz CRITICAL (~3-4h)

## C.1 D-001 Worker-Pos — bridge-handover Re-Sync-Sub-Typen (~1.5h)

**Source-File:** `skills/bridge-handover/SKILL.md` ODER `commands/bridge-handover.md`

**Patch — neue Sektion §Re-Sync-Sub-Typen einfügen:**

````markdown
## §Re-Sync-Sub-Typen (NEU v0.1.3)

`/bridge-handover --type=re-sync` differenziert zwei Sub-Typen via Body-Marker
und Pre-Flight-Pflicht:

### plan-layer-resync

**Inhalt:** Erwartung, Hypothesen, Plan über advisor-side / worker-side-Status.
**Pre-Flight-Pflicht:** keine zusätzliche.
**Pflicht-Body-Marker:**
```yaml
resync_sub_type: plan-layer
```

### execution-layer-resync

**Inhalt:** Verifikation tatsächlicher persistierter State, Round-Counter,
shared-path-Inhalte.
**Pre-Flight-Pflicht (NEU v0.1.3):** ≥1 reference muss `type=evidence` sein
mit advisor-side/worker-side-Pointer (z.B. via `mcp__session_info__read_transcript`
oder Filesystem-State-Pointer).
**Pflicht-Body-Marker:**
```yaml
resync_sub_type: execution-layer
evidence_pointers:
  - type: filesystem-state
    pointer: bridge/state.json#current_round
    verified_at: <ISO-timestamp>
```

### hybrid

**Inhalt:** beide Layer im selben Body.
**Pflicht-Body-Marker:**
```yaml
resync_sub_type: hybrid
```
Plus execution-layer-Pflicht erfüllt + plan-layer-Markierung im Body.

**Anti-Pattern:** re-sync ohne `resync_sub_type`-Marker = Layer-Konfusions-
Verdacht (F-RP-29). Pre-Flight 5 (NEU): WARN bei missing Marker.
````

**Plus: Pre-Flight 5 für re-sync in §Pre-Flight:**
```markdown
5. Re-Sync-Sub-Typ-Marker (NEU v0.1.3, nur bei type=re-sync):
   - Body MUSS resync_sub_type-Marker enthalten (plan-layer | execution-layer | hybrid)
   - Bei execution-layer/hybrid: ≥1 reference type=evidence Pflicht
   - Bei missing → WARN "Re-Sync-Sub-Typ-Konfusion-Verdacht (F-RP-29)"
```

**Acceptance:**
- Drei Sub-Typen dokumentiert mit Pflicht-Markern
- Pre-Flight 5 für re-sync implementiert
- Anti-Pattern-Hinweis F-RP-29 in beide Sektionen

**Self-Test:** T21-NEU (negative case: re-sync ohne Sub-Typ → WARN expected).

**Aufwand:** ~1.5h.

---

## C.2 D-001 Advisor-Pos — bridge-advisor §Plan-vs-Execution + Output-Marker (~2.5h)

**Source-File:** `skills/bridge-advisor/SKILL.md`

**Patch 1 — neue Sektion §Anti-Plan-Drift einfügen:**

````markdown
## §Anti-Plan-Drift (NEU v0.1.3, CRITICAL — F-RP-29)

bridge-advisor Skill darf NICHT detaillierten Plan-Text als Antwort an User
produzieren ohne nachfolgenden Skill-Aufruf. Empirisch validiert in
bridge-pair p3-real-user (4× Live-Reproduktion advisor-side R6→7, R7→8,
R10→11, R16→17).

**Format-Regel (Pflicht):**

| Plan-Text-Länge | Erlaubt? | Bedingung |
|---|---|---|
| ≤ 5 Sätze | OK | als Plan-Outline + sofortiger Skill-Aufruf |
| > 5 Sätze | NICHT OK | außer als Skill-Body bei tatsächlicher Skill-Invocation |

**Anti-Pattern AP-Plan-vs-Execution-Drift:**

Plan-Text > 5 Sätze ohne Skill-Aufruf = AP-Drift. Konsequenz:
- User interpretiert Plan-Text als Bridge-Write-done
- Worker erwartet Bridge-Artefakt
- F-RP-29 Plan-vs-Execution-Layer-Konfusion entsteht

**Pre-Flight-Erweiterung (NEU v0.1.3):**

Vor advisor-Skill-Output-Persistierung:
- Wenn Body Plan-Text-Pattern enthält ("ich werde", "geplant", "Inhalt der Round",
  "next steps", > 5 Sätze ohne konkrete State-Mutation)
- → WARN "AP-Plan-vs-Execution-Drift-Verdacht. Plan-Text ohne Skill-Aufruf
  reproduziert F-RP-29 (4× live in p3-real-user). Erwäge: kompakter
  Plan-Outline ≤5 Sätze + sofortige Skill-Invocation."
````

**Patch 2 — bridge-handover SKILL.md Output-Marker BRIDGE-WRITE-COMPLETED (Pflicht-Output-Block):**

```markdown
## §Output-Marker (NEU v0.1.3, F-RP-29 Korrektiv)

Skill darf NICHT erfolgreich-Output produzieren ohne folgenden Block:

```
============================================================
BRIDGE-WRITE COMPLETED — Round <n>
============================================================
artifact:           bridge/handover/<n>-<from>-<to>-<hash>.md
state.updated_at:   <timestamp>
state.current_round: <n>
phase:              <phase>
============================================================
```

Block referenziert verifizierbare Filesystem-Pointer. User sieht direkt
ob Bridge-Write erfolgte. Bei Skill-FAIL: NICHT diesen Block ausgeben,
sondern explicit FAIL-Diagnose.
```

**Patch 3 — bridge-advisor SKILL.md User-Translation-Konvention-Doku:**

````markdown
## §User-Translation-Konvention (NEU v0.1.3)

Wenn advisor-Session vermittelt zwischen User und Worker-Session via Mensch-
Translator: Konvention für Plan-vs-Done-Distinktion.

**advisor-Output an User darf nicht implizieren "Bridge-Write done" durch:**
- Hohen Detail-Grad ohne Skill-Aufruf-Marker
- Strukturierte Listen ohne `BRIDGE-WRITE COMPLETED`-Block
- Konkrete Ausführungs-Sprache ("ich schreibe", "geplant für Round X")
  ohne nachfolgendes Skill-Invocation

**Konvention für advisor-Output:**

| Modus | Marker am Anfang |
|---|---|
| Plan-Diskussion | `[plan-layer | no-bridge-write]` |
| Skill-Invocation | normal (Skill-Output enthält BRIDGE-WRITE-Block) |
| Status-Bericht ohne Skill | `[status-only | no-bridge-write]` |

User-side erwartet bei Forward an Worker:
- Bei `[plan-layer]`: User sagt "advisor diskutiert Plan, kein Bridge-Write"
- Bei BRIDGE-WRITE-Block: User sagt "advisor hat Round X geschrieben + persistiert"
- Bei `[status-only]`: User sagt "advisor evaluiert, kein Bridge-Write geplant"
````

**Acceptance:**
- bridge-advisor §Anti-Plan-Drift mit Pre-Flight-Pattern-Scan
- bridge-handover §Output-Marker mit Pflicht-Block
- bridge-advisor §User-Translation-Konvention mit Mode-Markern

**Self-Test:** T22-NEU (positive case: advisor-Output ohne BRIDGE-WRITE-Block → FAIL expected).

**Aufwand:** ~2.5h (3 Sektionen + Pattern-Scan + Self-Test).

---

## C.3 D-002 F-RP-32 — Pre-Flight 5 hard-enforce required-Args (~2h)

**Source-Files:** `skills/bridge-attach/SKILL.md` + `skills/bridge-handover/SKILL.md`

**Patch 1 — bridge-attach Pre-Flight 5:**

```markdown
5. Pflicht-Args-Validation (NEU v0.1.3, hard-enforce):
   - --worker-focus muss gesetzt sein (wenn role=worker)
   - --expertise-source muss gesetzt sein (wenn role=advisor)
   - Bei missing → ABBRUCH mit User-Question (NICHT Elicitation-Form-Fallback)
   - Diagnose-Output: "Pre-Flight FAIL Punkt 5 — required-Arg <name> missing.
     Skill-Spec verlangt hard-enforce v0.1.3+ (siehe F-RP-32). Bitte mit
     vollständigen Args erneut aufrufen."
```

**Patch 2 — bridge-handover Pre-Flight 5:**

```markdown
5. Type-spezifische Pflicht-Args-Validation (NEU v0.1.3, hard-enforce):
   - type=execute: --acceptance + --rollback + --wallclock-min Pflicht
   - type=decision-lock: --decided-by Pflicht
   - type=pre-patch: --acceptance + --wallclock-min Pflicht
   - type=execute / pre-patch: --acceptance muss valid JSON-Array sein
   - Bei missing → ABBRUCH mit Type-spezifischer Diagnose
```

**Patch 3 — bridge-init Pre-Flight 5 als Vorbild dokumentieren:**

```markdown
**Hinweis:** bridge-init Pre-Flight 5 (Profile-Validation) ist Vorbild für
hard-enforce-Logik. Keine Elicitation-Form-Fallback bei missing required.
Plugin-Robustheit darf nicht von Modell-Quality abhängen (F-RP-24
Marketplace-Adoption-Argument).
```

**Patch 4 — Skill-Doku-Updates für beide Skills:**

```markdown
## §Required-Args-Hard-Enforcement (NEU v0.1.3)

Required-Args werden in Pre-Flight 5 hard-enforced. Elicitation-Fallback ist
sekundär für optional-Args oder strukturierte Eingabe (z.B. JSON-Arrays für
acceptance_criteria), NICHT für missing required.

Begründung: Plugin-Robustheit-Garantie unabhängig von Modell-Verhalten
(F-RP-32 Mapping-Decision D-002 PATCH).
```

**Acceptance:**
- Beide Skills (bridge-attach + bridge-handover) Pre-Flight 5 hard-enforce
- §Required-Args-Hard-Enforcement-Sektion in beiden
- Diagnose-Output bei FAIL klar formuliert

**Self-Test:** T16-NEU (negative case: bridge-attach ohne `--worker-focus` → FAIL expected) + T17-NEU (negative case: bridge-handover --type=execute ohne `--acceptance` → FAIL expected).

**Aufwand:** ~2h (3 Skill-Edits + 2 Self-Tests + Doku-Updates).

---

# Phase D — Sentinel + Mount (~3-4h)

## D.1 F-RP-23 Pre-Test (Empirie-Verifikation, ~30min)

**Aufgabe:** verifizieren ob p3-real-user-Pilot Argument-Konsumption-Pfad
funktional war wegen:
- (a) Implementation-Bug in v0.1.2 Pre-Flight 4 (zu tolerant), oder
- (b) Plugin-Version-Path-Diff (z.B. lokale Modifikation ohne Schema-Spec-Match)

**Test-Sequenz:**

```bash
# 1. v0.1.2 Plugin-Version verifizieren
cat /Users/paulad/session-bridge/.claude-plugin/plugin.json | jq .version
# Erwartet: "0.1.2"

# 2. bridge-attach SKILL.md Pre-Flight 4 Source-Code lesen
cat /Users/paulad/session-bridge/skills/bridge-attach/SKILL.md | grep -A 20 "Pre-Flight 4"

# 3. P-RP-08 (Sentinel-Detection) und P-RP-01 (Sentinel-Stubs) verifizieren
git log --oneline | head -10  # Commit-History für 5bb36a9 Hotfix v0.1.1

# 4. Test-Init mit --worker-session-id direkt
mkdir -p /tmp/test-pair && cd /tmp/test-pair
# bridge-init --role=advisor --topic=test --worker-session-id=local_test
cat bridge/state.json | jq .roles.worker.session_id
# Erwartet bei v0.1.2 Spec-strict: "pending-attach"
# Tatsächlich p3-Pilot: "local_e9ba7337-..." (kein Sentinel)
```

**Expected Outcome:**
- Wenn Test-Init zeigt `roles.worker.session_id != "pending-attach"`: bestätigt p3-Befund, Spec-Inkonsistenz real
- Wenn Test-Init zeigt Sentinel: Plugin-Version-Path-Diff in p3-Pilot, Befund versionsabhängig

**Aufwand:** ~30min (Test-Setup + Verifikation).

---

## D.2 D-004 F-RP-23 PATCH — Sentinel-Invariante (~2-3h)

**Source-Files:** `skills/bridge-init/SKILL.md` + `skills/bridge-attach/SKILL.md`

**Patch 1 — bridge-init `--worker-session-id`-Behandlung:**

```markdown
## §--worker-session-id (REVIDIERT v0.1.3, F-RP-23 PATCH)

`--worker-session-id` ist jetzt **UX-Hint für Worker-Notification-Block**,
NICHT state-Pin.

**Verhalten v0.1.3+:**
- bridge-init schreibt IMMER `state.roles.worker.session_id = "pending-attach"`
  (Sentinel)
- `--worker-session-id` (falls übergeben) wird NUR für Worker-Notification-Block
  Generierung verwendet
- bridge-attach Pre-Flight 4 bleibt strikt auf Sentinel-String

**Migration für v0.1.2-Use-Cases:**
- Bestehende `state.json` mit direktem session_id-Pin: bridge-attach FAIL
- Workaround: state.json-Patch (session_id zurück auf "pending-attach")
- Empfehlung: Re-Init in v0.1.3+

**Begründung (D-004 R23-Position-Revidiert):**
- Sentinel-Pfad-Invariante als Spec-Kontrakt
- Plugin-Marketplace-Adoption-Argument konsistent mit D-002/D-005-A
- p3-Pilot-Empirie als Cross-Reference behalten (Implementation-Bug-Verdacht
  oder Version-Path-Diff in v0.1.2 Pre-Flight 4 Tolerance)
```

**Patch 2 — bridge-attach Pre-Flight 4 strict:**

```markdown
4. Sentinel-Detection (REVIDIERT v0.1.3, strict):
   - Akzeptiert NUR: state.worker.session_id == "pending-attach"
   - Bei Mismatch (z.B. direkter session_id-Pin aus v0.1.2-Pre-v0.1.3): FAIL
   - Diagnose: "Pre-Flight 4 FAIL — expected 'pending-attach', found '<X>'.
     Möglicher v0.1.2-Use-Case mit deprecated --worker-session-id-Pin.
     Empfehlung: state.json patch (session_id zurück auf 'pending-attach') und
     Re-attach. Siehe v0.1.3 Migration-Doku."
   - KEIN auto-recover-Branch (Option v2 verworfen via D-004 R23-Revidierung)
```

**Patch 3 — Self-Test T19-T21:**

```python
# T19 positive Sentinel-Pfad
def test_sentinel_path_v0_1_3():
    state = bridge_init(role="advisor", topic="test")
    assert state["roles"]["worker"]["session_id"] == "pending-attach"
    assert bridge_attach(pair_id, "worker") == "PASS"

# T20 negative direct-pin (v0.1.2 Use-Case nicht mehr unterstützt)
def test_direct_pin_v0_1_3_fails():
    state = bridge_init(role="advisor", topic="test", worker_session_id="local_test")
    # In v0.1.3+ wird --worker-session-id NUR für Notification verwendet,
    # NICHT als state-Pin
    assert state["roles"]["worker"]["session_id"] == "pending-attach"

# T21 negative attach mit existing direct-pin (Migration-Test)
def test_attach_with_legacy_pin_fails():
    state = manual_state_with_direct_pin("local_legacy")
    result = bridge_attach(pair_id, "worker")
    assert result == "FAIL"
    assert "Pre-Flight 4 FAIL" in result.diagnose
```

**Patch 4 — Migration-Doku in CHANGELOG:**

```markdown
## v0.1.3 — Migration-Hinweis F-RP-23

**Breaking-Change:** `--worker-session-id` ist jetzt UX-Hint, nicht state-Pin.

**v0.1.2-Use-Cases mit `--worker-session-id`-Direct-Pin:**
- Existierende state.json mit `roles.worker.session_id != "pending-attach"`
  müssen vor Re-Attach gepatched werden.
- Patch-Befehl: `jq '.roles.worker.session_id = "pending-attach"' state.json
  > state.tmp && mv state.tmp state.json`

**v0.1.3+ Standard-Use-Path:** `--worker-session-id` optional, wird für
Worker-Notification-Block verwendet aber state-Pin bleibt Sentinel.
```

**Acceptance:**
- bridge-init schreibt IMMER Sentinel
- bridge-attach Pre-Flight 4 strict
- T19-T21 Self-Tests
- Migration-Doku in CHANGELOG

**Aufwand:** ~2-3h (Skill-Edits + Self-Tests + Migration-Doku).

---

## D.3 D-005 Sub-A F-RP-15 PATCH — sandbox-mount-Pre-Flight (~1.5h)

**Source-File:** `skills/bridge-init/SKILL.md`

**Patch 1 — Pre-Flight 5 erweitert:**

```markdown
5. Profile-Validation (ERWEITERT v0.1.3 für F-RP-15):

   5.a Profile-Pfad existiert auf Host (Read-Tool-Check) — bestehender Test
   5.b Profile-Pfad sandbox-erreichbar (workspace-bash test -d <profile-path>)
       — NEU v0.1.3
       - Bei FAIL: WARN "Profile-Pfad nicht sandbox-mounted —
         Subprocess-Aufrufe werden scheitern. Add-Dir im Cowork-Project
         setzen oder Profile in Working-Dir verschieben."
       - WARN nicht hard-FAIL: bridge-init kann fortgesetzt werden, aber
         Profile-Subprocess-Loading wird zur Laufzeit scheitern
   5.c Profile-Frontmatter-Pflicht-Felder validieren — bestehender Test
```

**Patch 2 — neue Sektion §sandbox-mount-prerequisite:**

````markdown
## §sandbox-mount-prerequisite (NEU v0.1.3, F-RP-15)

Plugin-Use-Project Setup-Pflicht für `--expertise-profile`-Pfad:

- Profile-Pfad muss entweder im Cowork-Project Working-Dir liegen
  ODER als Add-Dir im Cowork-Project konfiguriert sein
- Sonst: Subprocess-Aufrufe via workspace-bash auf Profile-Files scheitern
  mit "No such file or directory" trotz Pre-Flight 5a PASS

**Empirische Validierung (bridge-pair p3-real-user):**
- Profile-Pfad: `/Users/paulad/session-bridge/private-notes/expertise-profiles/process-consulting/`
- Erforderte Add-Dir in beiden Cowork-Projects (advisor + worker)
- Workaround: jeweils Add-Dir setzen vor /bridge-init

**Setup-Vorschlag für künftige Plugin-Dev-Pilots:**

1. Cowork-Project erstellen mit Working-Dir
2. Add-Dirs setzen (mind. shared-path, ggf. Profile-Pfad falls nicht in Working-Dir)
3. /bridge-init aufrufen — Pre-Flight 5b prüft sandbox-Erreichbarkeit
4. Bei WARN 5b: Add-Dir-Setup nachholen, Re-Init
````

**Patch 3 — Self-Test T22-NEU:**

```python
def test_pre_flight_5b_sandbox_mount_warn():
    # Profile-Pfad existiert auf Host aber NICHT sandbox-mounted
    profile_path_host_only = "/tmp/test-host-only-profile"
    create_test_profile(profile_path_host_only)
    result = bridge_init(role="advisor", topic="test",
                         expertise_profile=profile_path_host_only)
    assert "WARN" in result.output
    assert "sandbox-mounted" in result.output
```

**Acceptance:**
- bridge-init Pre-Flight 5b sandbox-erreichbar mit WARN
- §sandbox-mount-prerequisite-Sektion
- T22-Self-Test

**Aufwand:** ~1.5h.

---

# Phase E — State-Schema + Tests + Doku (~2-3h)

## E.1 state-Schema v1.1.0 → v1.1.1 (~1h)

**Source-File:** `schemas/bridge_state_v1.json`

**Patch — Schema-Bump + neue Felder:**

```json
{
  "$id": "bridge_state_v1.json",
  "title": "session-bridge state schema",
  "description": "schema_version v1.1.1 — adds mapping_budget + mapping_category_history per Decision",
  "type": "object",
  "required": [
    "pair_id", "schema_version", "created_at", "updated_at", "phase",
    "roles", "topic", "current_round", "rounds", "open_blockers",
    "decision_log", "status_observations", "file_ownership",
    "shared_artifacts", "wallclock_estimates", "rollback_plan_path"
  ],
  "properties": {
    "schema_version": {
      "enum": ["1.0.0", "1.1.0", "1.1.1"]
    },
    "mapping_budget": {
      "type": "object",
      "description": "NEU v1.1.1: mapping-phase budget tracking",
      "properties": {
        "min": {"type": "integer"},
        "max": {"type": "integer"},
        "started_round": {"type": "integer"},
        "soft_cap": {"type": "boolean"},
        "rounds_per_befund": {"type": "integer"},
        "klarstellungs_reserve": {"type": "integer"},
        "triggers": {
          "type": "object",
          "properties": {
            "T1": {"type": "string"},
            "T2": {"type": "string"},
            "T3": {"type": "string"}
          }
        }
      }
    },
    "decision_log": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "round": {"type": "integer"},
          "decision": {"type": "string"},
          "rationale": {"type": "string"},
          "decided_by": {"type": "string", "enum": ["user", "consensus"]},
          "alternatives_considered": {"type": "array", "items": {"type": "string"}},
          "mapping_category_history": {
            "type": "array",
            "description": "NEU v1.1.1: audit-trail for position revisions",
            "items": {
              "type": "object",
              "properties": {
                "round": {"type": "integer"},
                "position": {"type": "string"},
                "by": {"type": "string"},
                "basis": {"type": "string"}
              }
            }
          }
        }
      }
    }
  }
}
```

**Patch — Migration-Test in Self-Test:**

```python
def test_v1_1_0_state_loads_in_v1_1_1():
    """v1.1.0 state.json without mapping_budget loads as v1.1.1 with default."""
    state = load_state(version="1.1.0")
    assert state["schema_version"] == "1.1.0"
    # v1.1.1 schema accepts v1.1.0 state (backward-compat)

def test_mapping_budget_field_optional():
    state = create_minimal_state(version="1.1.1")
    # mapping_budget is optional, not in required-list
    assert "mapping_budget" not in state or isinstance(state["mapping_budget"], dict)
```

**Aufwand:** ~1h.

---

## E.2 Self-Test smoke_self_test.py erweitern (~1h)

**Source-File:** `tests/smoke_self_test.py`

**Patch — neue Test-Cases T16-T22:**

```python
# T16: bridge-attach Pre-Flight 5 hard-enforce missing --worker-focus
def test_bridge_attach_pre_flight_5_missing_worker_focus():
    pass  # implementation per D-002

# T17: bridge-handover Pre-Flight 5 type=execute missing --acceptance
def test_bridge_handover_pre_flight_5_missing_acceptance():
    pass  # implementation per D-002

# T17b: bridge-handover Pre-Flight 6 convergence-skip missing marker
def test_bridge_handover_pre_flight_6_skip_without_marker():
    pass  # implementation per D-005-B

# T18: bridge-status output enthält alle Felder
def test_bridge_status_output_completeness():
    pass  # implementation per F-RP-31

# T19: bridge-init writes Sentinel always (auch mit --worker-session-id)
def test_bridge_init_always_sentinel_v1_1_3():
    pass  # implementation per D-004

# T20: bridge-worker WARN bei advisor-mode-tags
def test_bridge_worker_advisor_mode_drift_warn():
    pass  # implementation per F-RP-30

# T21: re-sync ohne resync_sub_type marker → WARN
def test_re_sync_missing_sub_type_warn():
    pass  # implementation per D-001-Worker

# T22: bridge-init Pre-Flight 5b sandbox-mount WARN
def test_bridge_init_pre_flight_5b_sandbox_mount_warn():
    pass  # implementation per D-005-A

# Erwartung: 22/22 PASS post-v0.1.3
```

**Aufwand:** ~1h (Test-Stubs + Implementation).

---

## E.3 CHANGELOG.md v0.1.3-Eintrag (~30min)

**Source-File:** `CHANGELOG.md`

**Patch — neuer Eintrag am Anfang nach v0.1.2-phase-b:**

```markdown
## [0.1.3] — 2026-04-29 — Plugin-Marketplace-Robustheit + F-RP-29-Disziplin

### Source: Bridge-Pair p3-real-user (Mapping-Phase R12-R26, 5 Decisions)

5 Mapping-Decisions D-001..D-005 covering 6 Items:
- D-001 F-RP-29 → DISSENS-DOCUMENTED §3.4.2 (Plan-vs-Execution-Layer-Konfusion)
- D-002 F-RP-32 → PATCH (Pre-Flight required-Args hard-enforce)
- D-003 F-RP-33 → AFFORDANCE (pre-allocated-Pattern-Doku)
- D-004 F-RP-23 → PATCH (Sentinel-Invariante, R23-revidiert nach Worker-Counter R22)
- D-005 Sub-A F-RP-15 → PATCH (sandbox-mount-Pre-Flight)
- D-005 Sub-B F-RP-34 → AFFORDANCE (Konvergenz-Skip-Konvention-Doku)

Plus existing v0.1.3-Backlog: F-RP-30 (Worker-Role-Boundary), F-RP-31
(User-Lifecycle-Visibility), F-RP-22 (Filesystem-Read), F-RP-24 (Title-statt-ID),
F-RP-25 (ID-Resolution).

### Added

- **bridge-handover §forward-pointer-rationale-Sektion** (D-003) — pre-allocated-
  Pattern für decision-lock vor Annex-Materialisierung
- **bridge-handover §konvergenz-skip-rationale + Pre-Flight 6** (D-005 Sub-B)
- **bridge-handover §Re-Sync-Sub-Typen + Pre-Flight 5** (D-001 Worker-Pos) —
  plan-layer / execution-layer / hybrid Differenzierung
- **bridge-handover §Output-Marker BRIDGE-WRITE-COMPLETED** (D-001 Advisor-Pos)
- **bridge-attach + bridge-handover Pre-Flight 5 hard-enforce** required-Args (D-002)
- **bridge-init Pre-Flight 5b sandbox-mount + §sandbox-mount-prerequisite** (D-005 Sub-A)
- **bridge-advisor §Anti-Plan-Drift + §User-Translation-Konvention** (D-001 Advisor-Pos, F-RP-29)
- **bridge-worker §Role-Boundary** (F-RP-30, CRITICAL) — keine Profile-pflicht-workflows
  oder AP-Diagnosen worker-side
- **bridge-status erweitert** (F-RP-31, CRITICAL) — User-friendly Output mit Rolle,
  Rounds, nächster Aktion, Polling-Hint
- **Skill-Mode-Marker [bridge-worker mode] / [bridge-advisor mode | profile=...]** (F-RP-31 Patch 4)

### Changed

- **bridge-init `--worker-session-id`** ist jetzt UX-Hint, nicht state-Pin (D-004,
  Breaking-Change). Migration-Doku siehe unten.
- **bridge-attach Pre-Flight 4** strict-Sentinel (D-004) — kein auto-recover-Branch.
- **state-Schema v1.1.0 → v1.1.1** — `mapping_budget` als top-level + `mapping_category_history`
  per Decision (E.1).

### Migration v0.1.2 → v0.1.3 (Breaking-Change F-RP-23)

`state.json` mit direktem session_id-Pin (v0.1.2-Use-Case mit `--worker-session-id`):
- Patch nötig: `jq '.roles.worker.session_id = "pending-attach"' state.json > state.tmp && mv state.tmp state.json`
- Anschließend: bridge-attach erneut ausführen

### Verification

- `claude plugin validate` PASS
- Self-Test 22/22 PASS (T16-T22 NEU)
- Bridge-Pair p3-real-user-Bilanz: `bridge/bilanz_8cbeaad0.md`
```

**Aufwand:** ~30min.

---

## E.4 BACKLOG.md Update (~30min)

**Source-File:** `BACKLOG.md`

**Patch — v0.1.3-Closure-Marker + Mapping-Items als closed:**

```markdown
**v0.1.3 Closure-Marker (2026-04-29):**

- 5 Mapping-Decisions aus Bridge-Pair p3-real-user gepatched (D-001..D-005)
- 4 existing-Backlog-Items gepatched (F-RP-30, F-RP-31, F-RP-22 partial, F-RP-25)
- Total: 11 Items, ~17-20h Self-Edit + 7 neue Self-Tests T16-T22
- Schema-Bump v1.1.0 → v1.1.1
- ADR_0030-Annex-Update: scope-lock-Phase mit Profile-Pin Spec-Default revidiert
  (drift_factor 2.4 in p3-Pilot)
- Bridge-Pair-Bilanz: `pilot-runs/p3-real-user/bridge/bilanz_8cbeaad0.md`
- v0.1.3-Patch-Pipeline-Doku: `pilot-runs/p3-real-user/v0.1.3-patch-pipeline.md`

**Phase-2 weiter aktiv** seit 2026-04-26. Tier-1-Items aus META_PROZESSE-Mining
(PB-001..PB-013) bleiben pending bei User-Pull-Trigger.
```

**Plus: Tier-2-Backlog-Befunde-Status-Update:**

```markdown
| ID | Befund | Severity | Status v0.1.3 |
|---|---|---|---|
| F-RP-15 | Mount-Inkonsistenz | HIGH | RESOLVED (D-005 Sub-A) |
| F-RP-23 | Sentinel-Bypass-Spec-Inkonsistenz | CRITICAL | RESOLVED (D-004) |
| F-RP-29 | Plan-vs-Execution-Layer-Konfusion | CRITICAL | DISSENS-DOCUMENTED §3.4.2 (D-001) |
| F-RP-30 | Worker-Skill-Role-Drift | CRITICAL | RESOLVED |
| F-RP-31 | User-Lifecycle-Visibility | CRITICAL | RESOLVED |
| F-RP-32 | Skill-Pre-Flight-Args | HIGH | RESOLVED (D-002) |
| F-RP-33 | pre-allocated-Pattern | BEOBACHTUNG | Affordance-Documented (D-003) |
| F-RP-34 | Konvergenz-Skip-Konvention | BEOBACHTUNG | Affordance-Documented (D-005 Sub-B) |
| F-RP-22 | Conversational-Memory-Cache | HIGH | partial RESOLVED (Pre-Flight 2 partial) |
| F-RP-24 | Title-statt-Session-ID | HIGH | DEFERRED-V0.1.4 |
| F-RP-25 | F-RP-XX-Placeholder-ID-Resolution | LOW | RESOLVED |
| F-RP-26 | worker.phase-Konsistenz | BEOBACHTUNG | DEFERRED-V0.1.4 |
| F-RP-19 | Visualization-Widget-UX | BEOBACHTUNG | DOCUMENTED (Phase A.3) |
```

**Aufwand:** ~30min.

---

## E.5 ADR_0030-Annex-Update (~30min)

**Source-File:** `docs/adr/ADR_0030_Expertise_Profile_Pattern.md`

**Patch — neuer Annex am Ende einfügen:**

```markdown
## Annex A — scope-lock-Phase-Spec-Default-Revidierung (post-p3-real-user-Pilot)

**Empirie aus Bridge-Pair p3-real-user (R0-R26):**

| Phase | ADR_0029-Default-Erwartung | p3-Pilot-Empirie | drift_factor |
|---|---|---|---|
| init + scope-lock + decision-lock-negotiations (R0-R11) | 4-6 Rounds | 12 Rounds | 2.4 |
| Mapping-Phase (R12-R26) | 14 Rounds (ad-hoc Spec) | 16 Rounds | 1.14 |

**Befund:** scope-lock-Phase mit Profile-Pin braucht 2-3× mehr Rounds als
ADR_0029-Default. Begründung:

1. **Profile-Pflicht-Workflows aktiviert** — `dissens-management-pflicht-bei-konsens-druck`
   produziert Counter-Sequenzen (R5→R6→R8→R9→R10)
2. **Konvergenz-Kriterium-Institutionalisierung** — Pflicht-explizite-pro-Punkt-Antwort
   verlangt mehrere Klarstellungs-Rounds
3. **Method-Annex-Substanz** — Profile-Frame-Anwendbarkeit muss substantiv
   geklärt werden (anwendbare vs un-anwendbare Frames)
4. **Counter-Konvergenz-Cycles** — substantielle Counter (R6 vier C-Punkte)
   produzieren 3-4 Re-Sync-Rounds bis Lock

**Spec-Default-Revidierung für künftige Plugin-Dev-Pilots:**

| scope-lock-Phase-Aspekt | Default v0.1.3+ |
|---|---|
| Rounds für init + scope-lock | 8-12 Rounds (statt 4-6) |
| Mapping-Rounds-pro-Befund | 2 Rounds (Decision + Konvergenz) |
| Klarstellungs-Reserve | 2 Rounds |
| Total Pair-Lifecycle für 5-6 Mapping-Items | 24-28 Rounds |

**Empfehlung:** ADR_0029 §5 Lifecycle-Sektion explizit auf Profile-Pin-Use-Cases
erweitern. Spec-Default soll Profile-Pflicht-Workflow-Overhead reflektieren.
```

**Aufwand:** ~30min.

---

# Phase F — Lower-Priority HIGH (~2-3h)

## F.1 F-RP-22 HIGH — Pre-Flight Punkt 2 Filesystem-Read (~1h)

**Source-File:** `commands/bridge-init.md` (oder `skills/bridge-init/SKILL.md`)

**Patch — Pre-Flight 2 mit Pflicht-Tool-Call:**

```markdown
2. state.json non-existent (PFLICHT-Tool-Call, NICHT Conversational-Memory):

   Bei JEDEM /bridge-init-Aufruf:
   ```bash
   mcp__workspace__bash 'test -f <shared-path>/bridge/state.json && echo EXISTS || echo MISSING'
   ```
   ODER (falls shared-path außerhalb sandbox):
   ```applescript
   mcp__Control_your_Mac__osascript 'do shell script "test -f <state.json> && echo EXISTS || echo MISSING"'
   ```

   Read-Result als Text-Output anzeigen ('filesystem read: state.json [exists|missing]').

   **NIEMALS** auf Conversational-Memory verlassen ("habe state.json vor X Min selbst erstellt").
```

**Plus T17-Self-Test:**

```python
def test_pre_flight_2_filesystem_read_post_external_cleanup():
    """In-Session-Re-Init nach external-cleanup soll state als MISSING erkennen."""
    init_pair()
    # External cleanup via osascript
    osascript("rm /tmp/test-pair/bridge/state.json")
    # Re-init in same session — Pre-Flight 2 muss MISSING erkennen, nicht Cache
    result = bridge_init(role="advisor", topic="test")
    assert "MISSING" in result.preflight_2_output
    assert result.success  # Pre-Flight 2 PASS weil non-existent
```

**Aufwand:** ~1h.

---

## F.2 F-RP-24 HIGH — `--worker-session-title` (~1.5h)

**DEFERRED auf v0.1.4** — dependency auf bridge-status-Erweiterung (F-RP-31)
+ Schema-Erweiterung. Markierung in BACKLOG.

**Falls in v0.1.3 doch implementiert (~1.5h):**

`skills/bridge-init/SKILL.md` Patch:

```markdown
## --worker-session-title (NEU v0.1.3, primäres UX-Flag)

`--worker-session-title="<title>"` als primäres Flag (statt --worker-session-id):

- Skill-Logic: `mcp__session_info__list_sessions` → exact-title-match
- Bei einem Match: ID intern resolven, Worker-Notification-Block zeigt Title
  (User-friendly) + ID (Debug-info)
- Bei multiple matches: strukturierte User-Disambiguation-Question
- Bei kein Match: User-Question "Title `<X>` nicht gefunden. Verfügbare Sessions: ..."

`--worker-session-id` bleibt als Fallback/Power-User-Flag (nicht primär dokumentiert).
```

**Aufwand:** ~1.5h falls in v0.1.3, sonst defer.

---

## F.3 F-RP-25 LOW — ID-Resolution (~30min)

**Source-File:** `skills/bridge-worker/SKILL.md`

**Patch — neue Pre-Flight für ID-Resolution-Konvention:**

```markdown
## §ID-Resolution-Pre-Flight (NEU v0.1.3, F-RP-25 Korrektiv)

Vor Status/Counter-Handover-Generierung mit Friction-Befund-Markierungen:

1. Wenn Body Friction-Befund-IDs erwähnt (`F-RP-XX`, `F-RP-YY` Placeholder):
   → Read setup-friction-log.md (oder analoges Plugin-Friction-Tracking)
   → ID-Lookup für aktuelle reale IDs
2. Bei Match: Placeholder durch reale ID ersetzen
3. Bei kein Match: User-Question "Friction-Befund `<XX>` nicht in friction-log
   gefunden. Welche reale ID?"
4. WARN-Mode (nicht hard-FAIL): Skill-Continuation erlaubt aber User-sichtbar
```

**Aufwand:** ~30min.

---

# §2 Test-Coverage post-v0.1.3

| Test | Quelle | Erwartet |
|---|---|---|
| T1-T15 | bestehend | PASS unverändert |
| T16 | D-002 bridge-attach Pre-Flight 5 missing required | FAIL erwartet |
| T17 | D-002 bridge-handover Pre-Flight 5 type=execute missing acceptance | FAIL erwartet |
| T17b | D-005-B Pre-Flight 6 convergence-skip without marker | WARN erwartet |
| T18 | F-RP-31 bridge-status output completeness | PASS erwartet |
| T19 | D-004 bridge-init always Sentinel | PASS (session_id == "pending-attach") |
| T20 | F-RP-30 bridge-worker advisor-mode-drift WARN | WARN erwartet |
| T21 | D-001-W re-sync without sub-type marker | WARN erwartet |
| T22 | D-005-A bridge-init Pre-Flight 5b sandbox-mount WARN | WARN erwartet |
| T17-FS-Read | F-RP-22 Pre-Flight 2 filesystem-read post-external-cleanup | MISSING erkannt |

**Total Self-Test post-v0.1.3:** 22+ Tests. Erwartung: alle PASS post-Patch-Welle.

---

# §3 Rollout-Sequence + Verification

## §3.1 Implementation-Reihenfolge (User-Workflow)

```
1. Cowork-Project öffnen: session-bridge-plugin-dev (Working-Dir /Users/paulad/session-bridge/)
2. v0.1.3-Branch anlegen: git checkout -b v0.1.3-mapping-decisions
3. Phase A (Quick-Wins) → commit
4. Phase B (CRITICAL Visibility) → commit
5. Phase C (CRITICAL Spec-Konsistenz) → commit
6. Phase D (Sentinel + Mount) → commit
7. Phase E (Schema + Tests + Doku) → commit
8. Phase F (Lower-Priority HIGH) → commit (oder defer auf v0.1.4)
9. Self-Test full-run: 22/22 PASS expected
10. claude plugin validate PASS
11. Plugin-Manifest version bump 0.1.2 → 0.1.3
12. Marketplace-Manifest version bump
13. Re-Pilot-Test in neuer Bridge-Pair (z.B. p4-test-v0.1.3) zur Falsifikation
14. Merge v0.1.3-branch → main
15. Tag v0.1.3 + push
```

## §3.2 Acceptance-Kriterien für v0.1.3-Closure

- ✓ alle 11 Items aus §1 Phasen A-E implementiert
- ✓ Phase F: F-RP-22 + F-RP-25 implementiert; F-RP-24 entweder implementiert oder DEFERRED-V0.1.4
- ✓ Self-Test 22+ Tests PASS
- ✓ claude plugin validate PASS
- ✓ Re-Pilot-Test: neue Bridge-Pair durchläuft Lifecycle ohne F-RP-29-Live-Reproduktion (advisor-side §Anti-Plan-Drift greift)
- ✓ CHANGELOG + BACKLOG aktualisiert
- ✓ ADR_0030-Annex-Update committet
- ✓ Bilanz-File und v0.1.3-Patch-Pipeline-Doku committet als Cross-Pair-Transfer-Material

---

# §4 Cross-Reference

| Dokument | Pfad | Zweck |
|---|---|---|
| Bilanz | `pilot-runs/p3-real-user/bridge/bilanz_8cbeaad0.md` | Pair-Lifecycle-Bilanz |
| Mapping-Decisions | `pilot-runs/p3-real-user/bridge/artifacts/mapping-decisions.md` v0.1.5 | 5 Decisions D-001..D-005 |
| Mapping-Method-Annex | `pilot-runs/p3-real-user/bridge/artifacts/mapping-method-annex.md` v0.1.2 | Methodischer Boden |
| Friction-Log | `pilot-runs/p3-real-user/setup-friction-log.md` | 16+ Friction-Befunde |
| Diese Datei | `pilot-runs/p3-real-user/v0.1.3-patch-pipeline.md` | Patch-Spec für Plugin-Dev-Session |
| Bridge-State | `pilot-runs/p3-real-user/bridge/state.json` | Pair-State (closed) |

---

**Status:** v0.1.3-Patch-Pipeline-Spec READY. User wendet Patches in separater Cowork-Session an.
